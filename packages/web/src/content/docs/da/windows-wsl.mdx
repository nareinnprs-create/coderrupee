---
title: Windows (WSL)
description: Kør CoderRupee på Windows med WSL for den bedste oplevelse.
---

import { Steps } from "@astrojs/starlight/components"

Selvom CoderRupee kan køre direkte på Windows, anbefaler vi at bruge [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install) for den bedste oplevelse. WSL giver et Linux-miljø, der fungerer problemfrit med CoderRupees funktioner.

:::tip[Hvorfor WSL?]
WSL giver bedre filsystemydelse, fuld terminalunderstøttelse og kompatibilitet med de udviklingsværktøjer, som CoderRupee er afhængig af.
:::

---

## Opsætning

<Steps>

1. **Installer WSL**

   Hvis du ikke allerede har gjort det, så [installer WSL](https://learn.microsoft.com/en-us/windows/wsl/install) med Microsofts officielle vejledning.

2. **Installer CoderRupee i WSL**

   Når WSL er sat op, skal du åbne din WSL-terminal og installere CoderRupee med en af [installationsmetoderne](/docs/).

   ```bash
   curl -fsSL https://coderrupee.ai/install | bash
   ```

3. **Brug CoderRupee fra WSL**

   Gå til din projektmappe (få adgang til Windows-filer via `/mnt/c/`, `/mnt/d/` osv.) og kør CoderRupee.

   ```bash
   cd /mnt/c/Users/YourName/project
   coderrupee
   ```

</Steps>

---

## Desktop-app + WSL-server

Hvis du foretrækker CoderRupee Desktop-appen, men vil køre serveren i WSL:

1. **Start serveren i WSL** med `--hostname 0.0.0.0` for at tillade eksterne forbindelser:

   ```bash
   coderrupee serve --hostname 0.0.0.0 --port 4096
   ```

2. **Forbind Desktop-appen** til `http://localhost:4096`

:::note
Hvis `localhost` ikke virker i din opsætning, så forbind i stedet med WSL-IP-adressen (fra WSL: `hostname -I`) og brug `http://<wsl-ip>:4096`.
:::

:::caution
Når du bruger `--hostname 0.0.0.0`, skal du sætte `CODERRUPEE_SERVER_PASSWORD` for at sikre serveren.

```bash
CODERRUPEE_SERVER_PASSWORD=your-password coderrupee serve --hostname 0.0.0.0
```

:::

---

## Webklient + WSL

For den bedste weboplevelse på Windows:

1. **Kør `coderrupee web` i WSL-terminalen** i stedet for PowerShell:

   ```bash
   coderrupee web --hostname 0.0.0.0
   ```

2. **Åbn fra din Windows-browser** på `http://localhost:<port>` (CoderRupee udskriver URL'en)

Når du kører `coderrupee web` fra WSL, får du korrekt filsystemadgang og terminalintegration, samtidig med at det stadig er tilgængeligt fra din Windows-browser.

---

## Adgang til Windows-filer

WSL kan få adgang til alle dine Windows-filer via `/mnt/`-mappen:

- `C:`-drev → `/mnt/c/`
- `D:`-drev → `/mnt/d/`
- Og så videre...

Eksempel:

```bash
cd /mnt/c/Users/YourName/Documents/project
coderrupee
```

:::tip
For den mest problemfri oplevelse kan du overveje at klone/kopiere dit repo ind i WSL-filsystemet (for eksempel under `~/code/`) og køre CoderRupee derfra.
:::

---

## Tips

- Lad CoderRupee køre i WSL for projekter, der ligger på Windows-drev - filadgangen er problemfri
- Brug VS Codes [WSL-udvidelse](https://code.visualstudio.com/docs/remote/wsl) sammen med CoderRupee for et integreret udviklingsworkflow
- Din CoderRupee-konfiguration og dine sessioner gemmes i WSL-miljøet i `~/.local/share/coderrupee/`
